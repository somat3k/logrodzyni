# ── Environment template for production ──────────────────────────────────────
# Populate via your secrets manager (Vault, AWS SSM, k8s Secrets, etc.)

NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# Generate with: openssl rand -base64 48
JWT_SECRET=REPLACE_WITH_STRONG_SECRET
JWT_EXPIRES_IN=8h
