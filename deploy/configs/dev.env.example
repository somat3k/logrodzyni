# ── Environment template for development ─────────────────────────────────────
# Copy to .env and adjust values.  Never commit .env to source control.

NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# JWT: use a long random secret in production.
JWT_SECRET=dev-only-change-me
JWT_EXPIRES_IN=8h
